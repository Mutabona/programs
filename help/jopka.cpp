double func(int a, int b, int c, int x) {
    if (x < 0 && b != 0) {
        return a - x/(10+b);
    }
    else if (x > 0 && b = 0) {
        return (x - a) / (x - c);
    }
    else {
        return 3*x + 2/c;
    }
}