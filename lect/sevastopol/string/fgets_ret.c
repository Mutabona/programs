//if(fgets(... )) // не NULL


 for (int i = 0; i < length; i++) 	
	  for (int j = length-1; j>0; j--) {
	                  pss[j] = pss[j-1];	// O(N^2)			 
	stack[vershina] = new_element; //push	
	vershina++;

	vershina--;	//pop

	if(vershina) //not empty
	
		stack[vershina - 1] //last in non-empty stack
