#include <vector>
#include <set>
#include <iostream>
#include <algorithm>
using namespace std;
int main()
{
vector<string> slovar;
slovar.resize(number);
cin >> slovar[i];
slovar[i][0] = toupper[i][0];
sort(slovar.begin(), slovar.end());

stack<int> stopka;
stopka.push(x);
cout << stopka.top();
cout << stopka.size();
stopka.pop();

set<long long> mnozhestvo();
mnozhestvo.insert(y);
cout << mnozhestvo.size();

multiset<unsigned long long> chastoty;
v.insert(z);
for(auto i:chastoty) //i for iterator
	cout << chastoty.count(i) << i;


	


