char* a[5];
float * p;

// Send begin
p = (char**) malloc(5 * sizeof(char*);   // alloc memory 
p[0] = a[0]; p[1] = a[1];
memcpy(p, &a, 5 * sizeof(*p));

       		// copy a[ ] into the allocated memory
// // Send end
//
